# BookingService - Test Data

Тестовые файлы и инструкции для проверки работы BookingService API.

## Содержимое

### 📋 TEST_PLAN.md
Комплексный план тестирования с описанием всех сценариев (80+ тестов):
- Получение свободных слотов
- Создание бронирований
- Отмена бронирований
- История пользователя
- Бронирования компании
- Конфигурация слотов
- Граничные случаи и race conditions

### 🚀 api_requests.sh
Исполняемый bash-скрипт с curl командами для тестирования API.

**Возможности:**
- Интерактивное меню для выбора групп тестов
- Smoke тесты (основные сценарии)
- Позитивные и негативные сценарии
- Автоматическая генерация дат (завтра, сегодня, вчера)
- Цветной вывод результатов
- Проверка доступности сервиса

---

## Быстрый старт

### 1. Подготовка окружения

```bash
# Запустить все сервисы
make docker-up

# Применить миграции и загрузить фикстуры
make db-reset
make fixtures
```

### 2. Проверка доступности сервиса

```bash
# Проверить, что сервис запущен
curl http://localhost:8083/metrics

# Должен вернуть Prometheus метрики
```

### 3. Запуск тестов

#### Вариант A: Интерактивный режим

```bash
make test-api

# Или напрямую
chmod +x test_data/api_requests.sh
./test_data/api_requests.sh
```

**В интерактивном меню:**
- `1-8` - выбрать группу тестов
- `s` - запустить smoke tests
- `a` - запустить все тесты
- `q` - выход

#### Вариант B: Smoke тесты

```bash
make test-smoke
```

Выполнит 4 основных теста:
1. Получение слотов
2. Создание бронирования
3. Получение бронирования
4. Проверка метрик

#### Вариант C: Отдельные тесты

```bash
# Загрузить функции
source test_data/api_requests.sh

# Запустить конкретный тест
tc_1_1    # Получение слотов на завтра
tc_2_1    # Создание бронирования
tc_3_1    # Получение бронирования

# Запустить группу тестов
run_group_1    # Available Slots (TC-1.*)
run_group_2    # Create Booking (TC-2.*)
```

---

## Структура тестов

### Group 1: Available Slots (TC-1.*)
- TC-1.1: Получение слотов на завтра
- TC-1.2: Слоты на сегодня (minBookingNoticeMinutes)
- TC-1.3: Слоты в выходной день
- TC-1.4: Услуга с индивидуальной конфигурацией
- TC-1.6-1.10: Негативные сценарии (404, 400)

### Group 2: Create Booking (TC-2.*)
- TC-2.1: Успешное создание
- TC-2.2: Создание с notes
- TC-2.3: Параллельное бронирование
- TC-2.4: Полностью занятый слот
- TC-2.7-2.15: Негативные сценарии

### Group 3: Get Booking (TC-3.*)
- TC-3.1: Получение своего бронирования
- TC-3.2: Попытка получить чужое (403)
- TC-3.3: Получение менеджером компании
- TC-3.4-3.5: Негативные сценарии

### Group 4: Cancel Booking (TC-4.*)
- TC-4.1: Отмена своего бронирования
- TC-4.2: Отмена без причины
- TC-4.3: Попытка отменить чужое (403)
- TC-4.4: Отмена менеджером

### Group 5: User Bookings (TC-5.*)
- TC-5.1: Все бронирования пользователя
- TC-5.2-5.3: Фильтрация по статусу
- TC-5.4: Попытка получить чужую историю (403)

### Group 6: Company Bookings (TC-6.*)
- TC-6.1: Все бронирования компании
- TC-6.2-6.6: Фильтрация (адрес, дата, период, статус)
- TC-6.7: Попытка доступа не-менеджером (403)

### Group 7: Company Config (TC-7.*)
- TC-7.1: Глобальная конфигурация
- TC-7.2: Конфигурация для адреса
- TC-7.3: Конфигурация для услуги на адресе
- TC-7.4: Конфигурация для услуги

### Group 8: Update Config (TC-8.*)
- TC-8.1: Обновление глобальной конфигурации
- TC-8.2: Создание конфигурации для адреса
- TC-8.4: Попытка обновления не-менеджером (403)

---

## Примеры использования

### Пример 1: Получение слотов на завтра

```bash
curl -s -X GET "http://localhost:8083/api/v1/companies/1/addresses/100/available-slots?serviceId=1&date=$(date -v+1d +%Y-%m-%d)" \
  -H "Content-Type: application/json" | jq .
```

### Пример 2: Создание бронирования

```bash
curl -s -X POST "http://localhost:8083/api/v1/bookings" \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 123456789,
    "companyId": 1,
    "addressId": 100,
    "serviceId": 1,
    "bookingDate": "2025-11-10",
    "startTime": "10:00",
    "notes": "Уделите внимание дискам"
  }' | jq .
```

### Пример 3: Получение бронирования

```bash
curl -s -X GET "http://localhost:8083/api/v1/bookings/1" \
  -H "X-User-ID: 123456789" | jq .
```

### Пример 4: История пользователя

```bash
curl -s -X GET "http://localhost:8083/api/v1/users/123456789/bookings?status=confirmed" \
  -H "X-User-ID: 123456789" | jq .
```

---

## Зависимости для тестирования

### Обязательные

- **curl** - для HTTP запросов
- **jq** - для форматирования JSON ответов

```bash
# macOS
brew install jq

# Ubuntu/Debian
sudo apt-get install jq

# Проверка
jq --version
```

### Опциональные

- **httpie** - альтернатива curl с красивым выводом

```bash
# macOS
brew install httpie

# Пример использования
http GET localhost:8083/api/v1/companies/1/addresses/100/available-slots serviceId==1 date==2025-11-10
```

---

## Интеграция с другими сервисами

### Требуются тестовые данные в UserService

BookingService ожидает, что пользователи существуют в UserService с выбранными автомобилями.

**Минимальный набор:**

```bash
# Пользователь 123456789 с BMW X5
curl -X POST http://localhost:8080/internal/users \
  -H "Content-Type: application/json" \
  -d '{
    "tgUserId": 123456789,
    "selectedCar": {
      "brand": "BMW",
      "model": "X5",
      "licensePlate": "А123БВ799"
    }
  }'

# Аналогично для 987654321, 111222333, и т.д.
```

### Требуются тестовые данные в SellerService

**Минимальный набор:**

```bash
# Компания 1 с адресами 100 и 101
# Услуги 1, 2, 3 с указанием addressIds
# Менеджер: 777777777
```

См. [../migrations/fixtures/README.md](../migrations/fixtures/README.md) для деталей.

---

## Troubleshooting

### Сервис недоступен

```
✗ BookingService недоступен на http://localhost:8083
```

**Решение:**
```bash
# Проверить статус
docker-compose ps

# Перезапустить
make docker-restart

# Посмотреть логи
make docker-logs-app
```

### jq не установлен

```
✗ jq не установлен. Установите: brew install jq
```

**Решение:**
```bash
brew install jq
```

### Ошибки 404 (company/service not found)

```
{"message": "company not found"}
```

**Причина:** Нет данных в SellerService.

**Решение:**
- Запустить SellerService
- Создать тестовые компании и услуги
- Проверить доступность SellerService из BookingService

### Ошибки 400 (no selected car)

```
{"message": "no selected car"}
```

**Причина:** Нет данных в UserService.

**Решение:**
- Запустить UserService
- Создать тестовых пользователей с автомобилями
- Проверить доступность UserService из BookingService

---

## Ожидаемые результаты

### Smoke Tests (4 теста)

- ✅ TC-1.1: Получение слотов → 200 OK, массив слотов
- ✅ TC-2.1: Создание бронирования → 201 Created, ID бронирования
- ✅ TC-3.1: Получение бронирования → 200 OK, детали бронирования
- ✅ Метрики → Prometheus метрики доступны

### Group 1: Available Slots (9 тестов)

- ✅ 5 позитивных сценариев → 200 OK
- ✅ 4 негативных сценария → 404, 400

### Group 2: Create Booking (9 тестов)

- ✅ 3 позитивных сценария → 201 Created
- ✅ 6 негативных сценариев → 400, 403, 404, 409

### Критерии успеха

- **Все smoke тесты проходят**
- **Позитивные сценарии возвращают корректные данные**
- **Негативные сценарии возвращают правильные HTTP коды**
- **Время ответа < 200ms для GET, < 500ms для POST**

---

## Следующие шаги

После успешного прохождения тестов:

1. **Запустить нагрузочные тесты** (опционально)
   ```bash
   # 100 параллельных запросов на слоты
   ab -n 100 -c 10 "http://localhost:8083/api/v1/companies/1/addresses/100/available-slots?serviceId=1&date=2025-11-10"
   ```

2. **Проверить метрики**
   ```bash
   curl http://localhost:8083/metrics | grep http_request_duration
   ```

3. **Задокументировать найденные баги** в TEST_RESULTS.md

4. **Подготовить к деплою**

---

## Дополнительные ресурсы

- [TEST_PLAN.md](TEST_PLAN.md) - детальный план тестирования
- [../migrations/fixtures/README.md](../migrations/fixtures/README.md) - описание фикстур
- [../schemas/schema.yaml](../schemas/schema.yaml) - OpenAPI спецификация
- [../CLAUDE.md](../CLAUDE.md) - полная документация проекта

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `make docker-logs-app`
2. Проверьте БД: `docker exec -it bookingservice-db psql -U postgres -d smc_bookingservice`
3. Пересоздайте окружение: `make db-reset && make fixtures`

**Happy Testing! 🚀**
